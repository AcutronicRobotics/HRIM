sudo: required
dist: xenial
services:
  - docker

before_script:
  - mkdir /tmp/hrim
  - mv * /tmp/hrim
  - cd .docker/ci

script:
  - docker build -t erlerobot/hrim .
  - docker run -v /tmp/hrim:/home/root/ros2_ws/src/hrim erlerobot/hrim